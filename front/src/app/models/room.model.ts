// import { renderFlagCheckIfStmt } from '@angular/compiler/src/render3/view/template';
import { TravelDate } from "./flight.model";

export interface RoomModel{
    currentBid: number;
    roomID: string;
    time: TravelDate;
}